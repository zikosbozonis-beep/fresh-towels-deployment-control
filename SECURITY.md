# Security policy

Do not open a public issue containing a credential, private configuration,
application artifact, customer information, database export or backup.

The repository intentionally accepts no arbitrary deployment command. Release
requests are data-only, short-lived and bound to exact identities and hashes.
Any parser, approval, artifact or remote-outcome ambiguity must fail closed.
